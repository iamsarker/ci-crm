# Software release ZIPs are private. They must only be served through the
# license-gated PHP download endpoints (never by direct URL).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
