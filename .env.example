# =============================================================================
# WHMAZ - CI-CRM Environment Configuration
# =============================================================================
#
# IMPORTANT:
# 1. Copy this file to .env in the root directory
# 2. Update database credentials with your actual values
# 3. Never commit .env to version control (it's in .gitignore)
#
# NOTE: All other application settings (SMTP, company info, billing, etc.)
# are stored in the database tables:
#   - app_settings: Site settings, SMTP configuration, company info
#   - sys_cnf: Key-value configuration pairs (cron keys, DNS settings, etc.)
#
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# These are the only settings required in .env file.
# Database credentials must be in .env because they're needed before
# the database connection is available.

DB_HOSTNAME=localhost
DB_PORT=3306
DB_DATABASE=whmaz_db
DB_USERNAME=whmaz_user
DB_PASSWORD=your_secure_database_password


# Google reCAPTCHA
RECAPTCHA_VERIFY_URL=https://www.google.com/recaptcha/api/siteverify

# =============================================================================
# SECURITY REMINDERS:
# 1. Never share your .env file
# 2. Never commit .env to version control
# 3. Use strong, unique database passwords
# 4. Backup .env file securely
# =============================================================================
